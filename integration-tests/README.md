# Integration Tests

Reference test suite with expected outputs generated by the [`poml`](https://github.com/microsoft/poml) Python package.

## Regenerate expected outputs

From the repository root:

```bash
uv run --python 3.11 --with poml python3 integration-tests/run-tests.py
```
